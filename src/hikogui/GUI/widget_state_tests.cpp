// Copyright Take Vos 2023.
// Distributed under the Boost Software License, Version 1.0.
// (See accompanying file LICENSE_1_0.txt or copy at https://www.boost.org/LICENSE_1_0.txt)

#include "widget_state.hpp"
#include <hikotest/hikotest.hpp>
#include <cstddef>

TEST_SUITE(widget_state) {

//TEST_CASE(iterate)
//{
//    // Iteration should iterate over each state once, and the value should match the index..
//    REQUIRE(hi::widget_state::begin() != hi::widget_state::end());
//
//    auto i = size_t{0};
//    for (auto it = hi::widget_state::begin(); it != hi::widget_state::end(); ++it) {
//        REQUIRE(i == static_cast<size_t>(it)); 
//        ++i;
//    }
//    
//    REQUIRE(i == hi::widget_state::size());
//}

};
